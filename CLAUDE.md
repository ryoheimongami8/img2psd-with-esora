# 作業ルール

## 絶対規則
1. `作成指示書_lineart2psd.md` が唯一の仕様書。書かれている API 仕様・バージョン・バイナリレイアウトを Web 検索で再確認しない。
2. `requirements.txt` にパッケージを追加しない。追加が必要だと判断した場合は、実装せず先にユーザーへ質問する。
3. `pytoshop` / `psd-tools` / `cairosvg` / `svglib` / `potrace` を import しない。
4. PSD は必ず無圧縮(compression=0)で書く。RLE/PackBits を実装しない。

## トークン節約規則
5. ファイル全体を読み直さない。編集は差分(Edit)で行う。新規作成時のみ全文を書く。
6. 画像・バイナリ・base64 文字列を標準出力に出さない。確認は `shape` / `dtype` / `len()` / `os.path.getsize()` のみ。
7. 検証は必ず 256×256 のダミー画像で行う。2048px で試行錯誤しない。
8. 1モジュール実装 → その場で単体検証 → 次へ、の順で進める。全部書いてから一括デバッグしない。
9. エラーが出たら、まず該当モジュールだけを見る。他ファイルを開かない。
10. 完了報告は5行以内。コードを再掲しない。

## 検証用ダミー画像の作り方(毎回これを使う)
python -c "import numpy as np, cv2; a=np.full((300,200,3),230,np.uint8); cv2.circle(a,(100,150),70,(20,20,20),3); cv2.line(a,(30,30),(170,270),(20,20,20),2); cv2.imwrite('tmp_test.png',a)"
